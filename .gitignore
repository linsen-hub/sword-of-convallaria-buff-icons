# PSD 源文件体积太大（单个 100MB+），不进版本控制；本地保留即可，跑合成脚本不需要它们
*.psd

# 临时预览/调试产物
_preview_tmp*/
*_预览放大.png

# 角色专属内容（原图/合成成品）可能含未发布游戏素材，不进这个通用工具仓库；
# 只保留目录结构（.gitkeep），本地照常生成/使用，不会被提交
/角色原图/*
!/角色原图/.gitkeep
/合成成品/*
!/合成成品/.gitkeep

# CHARACTERS 映射（角色名/技能名本身来自具体需求表，可能含未发布内容）不进版本
# 控制，只保留 characters.example.json 这个占位示例
/合成脚本/characters.json
