# 铃兰之剑图标自动化

《铃兰之剑》(Sword of Convallaria) 角色图标生产工具链：Buff/Debuff 图标合成、技能图标规范化、图文技能对照表生成、最终产物打包。

## 目录结构
```
铃兰图标自动化/
├── Buff-Debuff/            Buff/Debuff 图标合成（白色符号图 + 底色卡片模板）
├── 技能图标/                技能/个性图标规范化（AI定稿 → 128x128 游戏规范图）
├── 图文技能对照表/           生成角色图标图文对照联系表
└── 最终输出/                汇总打包脚本，把上面三者的成品复制到 <角色名>/ 平铺存放
```

四者通过角色名文件夹（如 `kelaiya`）对齐关联，不需要额外配置。

## 获取方式
```
git clone git@github.com:linsen-hub/sword-of-convallaria-buff-icons.git
```
（或在 GitHub 网页下载 ZIP）

**如果你之前已经 clone 过旧版（只有 Buff-Debuff 一个工具）的仓库，请重新 clone 到新目录，不要在旧目录里直接 `git pull`**——仓库根目录从 `Buff-Debuff/` 提升到了整个项目根目录，直接 pull 会导致路径嵌套错乱（变成 `Buff-Debuff/Buff-Debuff/...`）。旧目录里如果有你自己维护的本地配置文件（见下），先复制出来再删旧目录。

## 依赖安装（一次性）
```
pip install pillow numpy scipy
```

## 需要本地新建的配置文件
这些是角色专属数据（可能含未发布内容），**不进 git**，每人在自己机器上各自维护。做法统一：复制同目录下的 `*.example.json`，去掉 `.example` 改名。

真正需要手动填的只有一个：

| 工具 | 本地配置文件 | 作用 |
|---|---|---|
| Buff-Debuff | `Buff-Debuff/合成脚本/project_paths.json` | 你自己机器上 P4/资源目录的真实路径（查角色内容ID↔资源ID对照用），因人而异必须手填 |

其余几个（`characters.json` / `skill_characters.json` / `labels/<角色名>.json`）不用手动写——直接把 Jira 单或 Google 表格（`铃兰之剑2023-2026图标需求文档`）链接甩给 Cindy，说"帮我整理XX角色的技能图标"，Cindy 会自己读表、生成这几份配置、再跑脚本产出成品。

配完 `project_paths.json` 之后，直接跑各自目录下的脚本即可（脚本本身不用改）。

## 使用方式
每个工具目录下的 `.claude/skills/` 里有详细的技能说明文档。在 Cindy 里直接说"帮我整理XX角色的技能图标"，会自动按顺序跑完 Buff/Debuff → 技能图标 → 图文对照表 → 最终输出打包 这一整套流程（对应 `.claude/skills/character-icon-full-pipeline/SKILL.md`）。
